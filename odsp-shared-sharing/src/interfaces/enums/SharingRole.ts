/**
 * This enum was copied from odsp-next.
 */

const enum SharingRole {
    OWNER = 0,
    VIEW = 1,
    EDIT = 2,
    SUBMITTER = 3,
    CO_OWNER = 4,
    NONE = 5,
    CUSTOM = 6
}

export default SharingRole;