/**
 * This enum was copied from odsp-next.
 */

const enum PrincipalType {
    none = 0,
    user = 1,
    distributionList = 2,
    securityGroup = 4,
    sharepointGroup = 8
}

export default PrincipalType;