/**
 * This enum was copied from odsp-next.
 */

const enum SharingLinkKind {
    uninitialized = 0,
    direct = 1,
    organizationView = 2,
    organizationEdit = 3,
    anonymousView = 4,
    anonymousEdit = 5
}

export default SharingLinkKind;