// OneDrive:IgnoreCodeCoverage

/**
 * The client form type
 */
export const enum ClientFormType {
    /**
     * An existing item is being edited
     */
    editItem = 1,
    /**
     * A new item is being created
     */
    newItem = 2,
    /**
     * An existing item is being displayed
     */
    displayItem = 3
}
